@{
  Layout = "_Layout";
}
@using ModularityPro.Models;
@model List<ApplicationUser>
<div class="search-row">
@if (Model.Count == 0)
{
  <p>No users match this search.</p>
}
else{
  @foreach (ApplicationUser u in Model)
  {
    <div class="row">
      <div class="col-md-12">
        <div class="card">
          <div class="card-body">
            <div class="card-content">
              <img src = "@u.AvatarUrl" class="avatar">
              @Html.ActionLink($"{u.FirstName} {u.LastName}", "Index", "Profile", new {name = u.UserName})
            </div>
            <div class="card-icon">
              <img src="https://i.imgur.com/jnNc85n.png" class="add-friend-icon">
            </div>
          </div>
        </div>
      </div>
    </div>
  }
}
</div>